[build]
  base = "next-app"
  command = "npm ci && npx prisma migrate deploy && npm run build"
  publish = ".next"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "GEMINI_MODEL,SIDECAR_URL"

[[plugins]]
  package = "@netlify/plugin-nextjs"
