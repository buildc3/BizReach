DATABASE_URL=postgres://user:password@localhost:5432/lead_gen
HOST=127.0.0.1
PORT=3001
GOOGLE_PLACES_API_KEY=your_google_places_api_key_here
# Groq (free tier — get a key from https://console.groq.com)
GROQ_API_KEY=gsk_...
GROQ_MODEL=llama-3.3-70b-versatile
# WhatsApp is handled by the local Baileys sidecar (sidecar/index.js) on port 3099
# No API keys needed — just scan the QR code in Settings after starting the sidecar
